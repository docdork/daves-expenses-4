import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'daves-expenses-4',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
